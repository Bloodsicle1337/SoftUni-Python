def even_numbers_filter(numbers_list: list) -> list:
    evens = []
    for num in numbers_list:
        evens.append

    return evens
numbers = input().split()
result = even_numbers_filter(numbers)

print(result)